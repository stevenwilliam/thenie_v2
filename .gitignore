# scratch / local
*.log
.DS_Store
node_modules/
.env
.env.*
!.env.example

# build output — regenerate with scripts/build-site.sh
dist/

# harness-local config, not project content
.claude/

# Backend secrets — never in git. .env.example is the documented surface.
.env
.env.*
!.env.example

# Go build output
/server/bin/
